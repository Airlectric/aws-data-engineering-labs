# Terraform provider cache and state
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*

# Local variable files — copy terraform.tfvars.example and fill in real values
terraform.tfvars

# OS noise
.DS_Store
Thumbs.db
